[project]
name = "streaming-engine"
version = "0.1.0"
description = "PySpark Structured Streaming fraud detection"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=3.5.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
