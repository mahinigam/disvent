global:
  scrape_interval: 10s

scrape_configs:
  - job_name: disvent-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8001

  - job_name: redpanda
    metrics_path: /public_metrics
    static_configs:
      - targets:
          - redpanda:9644
